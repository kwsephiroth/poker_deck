#include <iostream>
#include "../include/Deck.h"

int main()
{
	Poker::Deck deck;
	//deck.shuffle();
	//deck.shuffle();
	//deck.shuffle();
	//
	//for (int i = 0; i < 53; ++i)
	//{
	//	auto dealt_card = deck.deal_card();
	//	if(dealt_card)
	//		std::cout << "Dealt Card = " << *dealt_card << std::endl;
	//}
	//deck.print(); std::cout << "\n";
	//auto hand = deck.deal_cards(5);
	//std::cout << "Hand Size = " << hand.size() << std::endl;
	//deck.print(); std::cout << "\n";
	//deck.replace_cards(hand);
	//deck.print(); std::cout << "\n";
	//std::cout << "Hand Size = " << hand.size() << std::endl;
	//std::cout << "New Deck Size = " << deck.size() << std::endl;
	//for (const auto& dealt_card : hand)
	//{
	//	if(dealt_card)
	//		std::cout << "Dealt Card = " << *dealt_card << std::endl;
	//}
	//deck.print(); std::cout << "\n";
	//std::cout << "Dealt Card = " << *deck.deal_card_from_position(4) << std::endl;
	//deck.print(); std::cout << "\n";

	return 0;
}